 Article stored in PostgreSQL
 Description enrichment completed before queueing
 Tags saved
 Article ID queued
 BullMQ queue configured
 Embedding worker created
 OpenAI embedding client created
 text-embedding-3-small configured
 pgvector configured
 Embedding persisted
 Duplicate embedding handled with article ID
 Vector similarity API
 Similar articles UI
 Story clustering
 Source comparison

 BRANCHES
 main
 │
 ├── develop
 │
 ├── feature/react-news-api
 │
 ├── feature/semantic-search
 │
 ├── feature/story-clustering
 │
 └── feature/source-comparison

 ┌──────────────────────────────────────────────┐
│                    Internet                  │
└──────────────────────┬───────────────────────┘
                       │
                pulsevow.com
                       │
              ┌────────┴────────┐
              │                 │
        React Frontend       api.pulsevow.com
              │                 │
              │            Node + Express
              │                 │
              │       ┌─────────┴─────────┐
              │       │                   │
              │   PostgreSQL           Redis
              │                           │
              │                      BullMQ
              │                           │
              │                       AI Worker
              │
              └────────── Browser

Phase 1 — You

Backend

Finish article APIs
Finish embedding pipeline
Make API production-ready
CORS
authentication/security basics
error handling
pagination
API response DTOs

Infrastructure

PostgreSQL
Redis
BullMQ worker
Docker
domain
HTTPS
api.pulsevow.com
Phase 2 — You

Connect your React app to:

https://api.pulsevow.com

and remove the hardcoded/mock news data.

Phase 3 — Deploy
pulsevow.com
      ↓
React

api.pulsevow.com
      ↓
Express
      ↓
PostgreSQL / Redis / Workers
Phase 4 — Handoff

Frontend developer:

"Here is the React application and API contract. Use api.pulsevow.com for backend data."

Backend developer:

"Here is the backend repository, architecture, DB schema and worker setup."

You:

Owner / architect / PR reviewer
